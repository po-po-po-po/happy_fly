<!--列表页-->
<view class='container'>
  <!--等待框-->
  <!--顶部标题栏-->
  <view class='page-header'>
   <text class='page-title'>航班查询</text>
  </view>
     <view class='item'>
       <image class='poster' src='https://www.potucs.com/wechat/fly/6.jpg'></image>
          <view class='meta'>
            <text class='title'>随心飞航班</text>
              <navigator url="../city/city" class="mod-item">
          <view class="follow1_1 {{item.isFollow ? 'follows' : ''}}" data-id="{{item._id}}" bindtap="handleFollow">
           全国
          </view>
           </navigator>
 </view>
 </view>


  <!--列表-->
  <view  wx:for="{{list}}" wx:for-index="bindex"  wx:key="id">
    <!--另一种情况-->
    <!--类型判断,显示不同的数据-->
        <view class='item'>
       <image class='poster' src='{{item.flyIcon}}'></image>
          <view class='meta'>
            <text class='title'>{{item.flyName}}</text>

       <block wx:if="{{item.id == 1}}">
        <!--
              <navigator url="../fly67/fly67?day=6" class="mod-item">
                  <view class="follow1_2 {{item.isFollow ? 'follows' : ''}}" data-id="{{item._id}}" bindtap="handleFollow">
                周六
                  </view>
                      </navigator>
-->
               <view class='zhe'>
                 <navigator url="../fly67/fly67?day=7" class="mod-item">
          <view class="follow1_1 {{item.isFollow ? 'follows' : ''}}" data-id="{{item._id}}" bindtap="handleFollow">
        周末
          </view>
              </navigator>
                      
     <navigator url="../fly67/fly67?day=7" class="mod-item">
          <view class="follow1_1 {{item.isFollow ? 'follows' : ''}}" data-id="{{item._id}}" bindtap="handleFollow">
        早晚
          </view>
              </navigator>

                   <navigator url="../fly67/fly67?day=7" class="mod-item">
          <view class="follow1_1 {{item.isFollow ? 'follows' : ''}}" data-id="{{item._id}}" bindtap="handleFollow">
        大兴
          </view>
              </navigator>
</view>


               <view class='zhe'>
                 <navigator url="../fly67/fly67?day=7" class="mod-item">
          <view class="follow1_1 {{item.isFollow ? 'follows' : ''}}" data-id="{{item._id}}" bindtap="handleFollow">
        西域
          </view>
              </navigator>
                      
     <navigator url="../fly67/fly67?day=7" class="mod-item">
          <view class="follow1_1 {{item.isFollow ? 'follows' : ''}}" data-id="{{item._id}}" bindtap="handleFollow">
        大湾区
          </view>
              </navigator>

</view>

              </block>

                
    <block wx:if="{{item.id == 2}}">
                  <view class='c9'>
              <navigator url="../flycq/flycq?airportCode=SJW" class="mod-item">
                  <view class="follow1_2 {{item.follow1_2 ? 'follow4' : ''}}" data-id="{{item._id}}" bindtap="handleFollow">
                  东航早8点
                  </view>
                      </navigator>
                                <navigator url="../flycq/flycq?airportCode=ZQZ" class="mod-item">
                  <view class="follow1_2 {{item.follow1_2 ? 'follow4' : ''}}" data-id="{{item._id}}" bindtap="handleFollow">
                东航晚8点
                  </view>
                      </navigator>
                   <navigator url="../flycq/flycq?airportCode=HDG" class="mod-item">
                  <view class="follow1_3 {{item.follow ? 'follow4' : ''}}" data-id="{{item._id}}" bindtap="handleFollow">
                全部
                  </view>
                      </navigator> 
                                   
                      </view>

                       
              </block>
              
               
          </view>
        </view>
  
  </view>
</view>