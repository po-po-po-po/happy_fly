<!--列表页-->
<view class='container'>
  <!--等待框-->
  <!--顶部标题栏-->
  <view class='page-header'>
   <text class='page-title'>随心飞查询</text>
  </view>
  <!--列表-->
  <view  wx:for="{{list}}" wx:for-index="bindex"  wx:key="id">
    <!--另一种情况-->
    <!--类型判断,显示不同的数据-->
        <view class='item'>
       <image class='poster' src='{{item.flyIcon}}'></image>
          <view class='meta'>
            <text class='title'>{{item.flyName}}</text>
       <block wx:if="{{item.id == 1}}">
        <!--
              <navigator url="../fly67/fly67?day=6" class="mod-item">
                  <view class="follow1_2 {{item.isFollow ? 'follows' : ''}}" data-id="{{item._id}}" bindtap="handleFollow">
                周六
                  </view>
                      </navigator>
-->
              <navigator url="../fly67/fly67?day=7" class="mod-item">
          <view class="follow1_1 {{item.isFollow ? 'follows' : ''}}" data-id="{{item._id}}" bindtap="handleFollow">
        周日
          </view>
              </navigator>
              </block>
       <block wx:if="{{item.id == 2}}">
              <navigator url="../activity/activity?airline_code={{item.airlinesCode}}" class="mod-item">
                  <view class="follow {{item.isFollow ? 'follows' : ''}}" data-id="{{item._id}}" bindtap="handleFollow">
                早8前航班查询
                  </view>
                      </navigator>
              <navigator url="../activity/activity?airline_code={{item.airlinesCode}}" class="mod-item">
          <view class="follow {{item.isFollow ? 'follows' : ''}}" data-id="{{item._id}}" bindtap="handleFollow">
        晚8后航班查询
          </view>
              </navigator>
              </block>

                  <block wx:if="{{item.id == 3}}">

                  <view class='zhe'>
              <navigator url="../flycq/flycq?airportCode=NGB" class="mod-item">
                  <view class="follow3 {{item.follow3 ? 'follow3' : ''}}" data-id="{{item._id}}" bindtap="handleFollow">
                宁波
                  </view>
                      </navigator>
                      
              <navigator url="../flycq/flycq?airportCode=HGH" class="mod-item">
          <view class="follow3 {{item.follow3 ? 'follow3' : ''}}" data-id="{{item._id}}" bindtap="handleFollow">
        杭州
          </view>
              </navigator>
</view>



<view class='zhe'>

                   <navigator url="../flycq/flycq?airportCode=HSN" class="mod-item">
          <view class="follow3 {{item.follow3 ? 'follow3' : ''}}" data-id="{{item._id}}" bindtap="handleFollow">
        舟山
          </view>
              </navigator>


                   <navigator url="../flycq/flycq?airportCode=WNZ" class="mod-item">
          <view class="follow3 {{item.follow3 ? 'follow3' : ''}}" data-id="{{item._id}}" bindtap="handleFollow">
        温州
          </view>
              </navigator>

           </view>   

              
               
              </block>




                <block wx:if="{{item.id == 4}}">

                  <view class='zhe'>
              <navigator url="../flycq/flycq?airportCode=NKG" class="mod-item">
                  <view class="follow4 {{item.follow4 ? 'follow4' : ''}}" data-id="{{item._id}}" bindtap="handleFollow">
                南京
                  </view>
                      </navigator>
                      
              <navigator url="../flycq/flycq?airportCode=CZX" class="mod-item">
          <view class="follow4 {{item.follow4 ? 'follow4' : ''}}" data-id="{{item._id}}" bindtap="handleFollow">
        常州
          </view>
              </navigator>

                 <navigator url="../flycq/flycq?airportCode=YNZ" class="mod-item">
          <view class="follow4 {{item.follow4 ? 'follow4' : ''}}" data-id="{{item._id}}" bindtap="handleFollow">
        盐城
          </view>
              </navigator>

              
</view>



<view class='zhe'>

                   <navigator url="../flycq/flycq?airportCode=NTG" class="mod-item">
          <view class="follow4 {{item.follow4 ? 'follow4' : ''}}" data-id="{{item._id}}" bindtap="handleFollow">
        南通
          </view>
              </navigator>


                   <navigator url="../flycq/flycq?airportCode=HIA" class="mod-item">
          <view class="follow4 {{item.follow4 ? 'follow4' : ''}}" data-id="{{item._id}}" bindtap="handleFollow">
        淮安
          </view>
              </navigator>

                   <navigator url="../flycq/flycq?airportCode=YTY" class="mod-item">
          <view class="follow4 {{item.follow3 ? 'follow4' : ''}}" data-id="{{item._id}}" bindtap="handleFollow">
        扬州
          </view>
              </navigator>


           </view>   

              
               
              </block>






              
                <block wx:if="{{item.id == 5}}">
                  <view class='zhe'>
              <navigator url="../flycq/flycq?airportCode=SHE" class="mod-item">
                  <view class="follow5 {{item.follow4 ? 'follow4' : ''}}" data-id="{{item._id}}" bindtap="handleFollow">
                沈阳
                  </view>
                      </navigator>
                      </view>
              </block>


          <block wx:if="{{item.id == 6}}">
                  <view class='zhe'>
              <navigator url="../flycq/flycq?airportCode=LHW" class="mod-item">
                  <view class="follow6 {{item.follow4 ? 'follow4' : ''}}" data-id="{{item._id}}" bindtap="handleFollow">
                兰州
                  </view>
                      </navigator>
                      </view>
              </block>


                           <block wx:if="{{item.id == 7}}">
                  <view class='zhe'>
              <navigator url="../flycq/flycq?airportCode=SWA" class="mod-item">
                  <view class="follow7 {{item.follow4 ? 'follow4' : ''}}" data-id="{{item._id}}" bindtap="handleFollow">
                潮汕
                  </view>
                      </navigator>
                      </view>
              </block>



          </view>
        </view>
  
  </view>
</view>